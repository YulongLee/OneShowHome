# OneShowHome

OneShowHome 项目代码仓库。

## 开发约定

- `main` 始终保持可发布状态。
- 功能开发使用 `feature/<name>` 分支，修复使用 `fix/<name>` 分支。
- 不提交密码、密钥、访问令牌或本地 `.env` 文件。
- 提交信息使用 `feat:`、`fix:`、`docs:`、`refactor:`、`test:`、`chore:` 等前缀。
- 合并前完成格式检查、测试和构建验证。

